1) Add a quick in-panel diagnostics copy button for resolved combat setting targets to simplify support reports.
2) Add an explicit "Applied vs Previewing" badge in dropdown labels for clearer pending-state visibility.
3) Add optional per-character recent preview history persistence (with account-wide fallback).
4) Add a compact read-only custom-font availability summary (loaded/missing count) in the Custom category tooltip.
